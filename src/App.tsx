import React from 'react'
import ReactDOM from 'react-dom/client'
import './style/app.css'
import './style/import.scss'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <h1 className="text-xl font-bold overline">
      Hello world!
    </h1>
  </React.StrictMode>,
)
